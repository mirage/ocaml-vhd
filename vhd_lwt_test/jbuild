(executable
  ((name parse_test)
   (libraries
     (cstruct
      disk
      io-page.unix
      lwt
      lwt.unix
      oUnit
      vhd
      vhd-lwt))
   (preprocess (pps (lwt.ppx)))))

(alias
  ((name   runtest)
   (deps   (parse_test.exe))
   (action (run ${<}))))
